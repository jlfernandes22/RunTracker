/**
 * Post-install patch hook.
 *
 * The original patch for expo-document-picker was never committed to the
 * repository. This placeholder keeps the `postinstall` npm hook green.
 * Re-add any required patching here if a document-picker workaround is needed.
 */
console.log('[patch-document-picker] no-op');
